simulation_params:
  initial_step_time: 0
  step_time_interval: 1
  final_step_time: 2

simulation_steps:
  # we describe here objects with schedule for which time points they are active
  - time_points: [1]
    generators:
      - sequence:
        - grow
